<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="finalproject.csci205.com.countdown">


    <permission android:name="android.permission.MEDIA_CONTENT_CONTROL" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <service android:name="finalproject.csci205.com.countdown.Service.CountDownService">

        </service>
        <receiver
            android:name=".View.CountDownView$NotificationButtonListener"
            android:exported="true">
        </receiver>

        <intent-filter>
            <action android:name="android.intent.action.Start" />
            <action android:name="android.intent.action.Pause" />
            <action android:name="android.intent.action.Cacnel" />
        </intent-filter>
    </application>

</manifest>